# Straddly — Python dependencies
# Install: pip install -r requirements.txt

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Database
asyncpg==0.29.0

# HTTP client (for DhanHQ REST calls)
httpx==0.27.0

# WebSocket client (for DhanHQ WS connections)
websockets==12.0

# Config / settings
pydantic==2.7.1
pydantic-settings==2.2.1

# TOTP for automatic DhanHQ token renewal (pyotp.TOTP)
pyotp==2.9.0

# Password hashing (bcrypt — replaces plain SHA-256)
bcrypt==4.2.1

# Encryption (Phase 10: For secret at-rest encryption)
cryptography==42.0.7

# Timezone
# (zoneinfo is stdlib in Python 3.9+; tzdata needed on Windows/Docker)
tzdata==2024.1
pytz==2024.1
